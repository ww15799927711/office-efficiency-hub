# 最佳实践与避坑指南 (Best Practices & Anti-Patterns)

本文件沉淀 office-efficiency-hub 的"做得好 / 别这么干"经验库（吸收自 WPS 全家桶的"代码生成模板 + 最佳实践案例 + 避坑指南"结构）。所有条目均来自真实办公场景踩坑与验证。

---

## 一、10 个最佳实践

1. **先标准化再自动化**：脏流程上套 AI 只会加速混乱。先理清结构、统一口径（尤其财务科目表、日期格式），再让 skill 生成。
2. **一句话触发多场景**：同一个输入尽量一次串起多个场景（如"整理这场会 + 列行动项 + 发确认邮件 + 进周报"），利用数据链复用，避免四个孤立任务各做一遍。
3. **数据体检先于一切计算**：任何透视/统计前先查缺失、重复、类型、单位、口径。漏掉这步，结论全是错的。
4. **输出 WPS/MS 通用的 OOXML**：生成的 .docx/.xlsx/.pptx 一律用 Office Open XML，WPS 与 MS Office 双向兼容；不要特意绑定某一家。
5. **失败自动重试 + 降级**：调外部命令/连接器失败先重试 3 次（递增间隔），再降级到兜底方案（如未装 OCR 技能就提示安装或退化 pdf），绝不静默丢弃。
6. **大文件分块处理**：单文件 ≥ 50MB 不要一次性载入；分块读取、分批产出，配合硬件自适应（CPU/内存）调整超时与并发，避免 OOM。
7. **财务/合同必须附免责声明**：AI 审查与识别仅为辅助，金额以票面为准、合同以执业法务复核为准，交付物强制带免责声明。
8. **红涨绿跌 + 简体中文默认**：中国用户场景默认简体中文；股市/财务涨跌图表遵循红涨绿跌，不套用欧美绿涨红跌。
9. **产物主动沉淀知识库**：每次交付后询问是否进 §5 知识库；常用条款、科目映射、客户卡、模板片段变成可复用资产，打通信息孤岛。
10. **接入优先、导出兜底**：协作类软件有 API 就调连接器直接拉；ERP 闭源无 API 只处理导出文件；未授权连接器统一降级导出，不伪造接口。

---

## 二、20+ 避坑指南（反模式 FAQ）

**识别与分流**
- ❌ "这段会议录音转文字后直接当纪要发" → ✅ 必须经 §1 提取决策/行动项/风险，口语转写不等于纪要。
- ❌ "把 PDF 截图里的表格手动敲进 Excel" → ✅ 先 §13 OCR 提取，输出 Markdown 表格+CSV，再进 xlsx 透视。
- ❌ "邮件和纪要混在一起让我都整理" → ✅ 歧义判定：邮件与纪要混用优先纪要；先厘清意图再动手。

**数据分析（§12）**
- ❌ "直接对原始表做透视，没看缺失值" → ✅ 数据体检必做；缺失/重复会让聚合结果 silently 偏离。
- ❌ "用平均值代表有极端值的收入分布" → ✅ 用中位数抗极值，或截断后对比。
- ❌ "图表不带单位、期间、口径脚注" → ✅ 每张图必须有标题+坐标轴单位+期间+口径，否则无法复核。
- ❌ "用虚构数字凑结论" → ✅ 禁止臆造；结论必须来自真实计算，附"数据口径与局限"。
- ❌ "用户说'分析下销售'就闷头算所有维度" → ✅ 先做意图识别（§12 步骤0），匹配用户真正想要的那类分析。

**文档/PPT（§6/§2）**
- ❌ "Word 文档照搬进 PPT，一页塞满" → ✅ 每页一个核心观点+2-4 支撑要点；长文档合并次级标题、提炼而非照搬。
- ❌ "总结把原文数字改了'更顺口'" → ✅ 保留关键数据/人名/结论，严禁改写事实。
- ❌ "长文一次性 Read 到底导致截断" → ✅ 分段读取+Grep，保持上下文完整。

**财务/ERP（§10/§15）**
- ❌ "让 skill 直接登录金蝶录凭证" → ✅ 闭源无 API，绝不直连；只处理导出数据，产出凭证草稿由用户入账。
- ❌ "税率随便填、税号不校验" → ✅ 税率与货物匹配、税号 15/18/20 位合法、购买方与本单位一致。
- ❌ "发票 OCR 后直接入账不复核" → ✅ 重号检测+三栏勾稽+免责声明，关键金额以票面为准。
- ❌ "报表勾稽关系不校验就交" → ✅ 资产=负债+权益、净利润勾稽必须过，否则财报不可信。

**合同（§14）**
- ❌ "AI 审完就签字" → ✅ 仅辅助参考，重大合同交执业律师/法务最终复核，结果带免责声明。
- ❌ "只看法务视角漏了采购/财务" → ✅ 三视角全覆盖：通用结构+采购+法务+财务。

**邮件/沟通（§3）**
- ❌ "拒绝对方邮件开头就否定" → ✅ 先肯定再转折给台阶；中文商务主题须点明事由。
- ❌ "草拟完直接发出" → ✅ 附发送前检查清单：收件人/附件/时限/语气。

**工程稳定性**
- ❌ "调连接器一次失败就报错放弃" → ✅ 自动重试 3 次（1s→3s→5s）再报。
- ❌ "50MB+ 文件一次性载入" → ✅ 分块读取、硬件自适应降级，避免 OOM。
- ❌ "大批量任务失败从头再来" → ✅ 支持断点续跑/检查点恢复。
- ❌ "输出格式只认某一家 Office" → ✅ 四引擎识别，默认纯 Python 生成 OOXML，跨平台最稳。

**知识沉淀（§5）**
- ❌ "产物交付完就结束" → ✅ 主动询问是否沉淀，去重保留一份，旧版标注归档。
- ❌ "企业知识库擅自改结构" → ✅ 按平台连接器/API 规范写入，不改结构。

**外部依赖**
- ❌ "假设公共 API 永远可用" → ✅ 本 skill 不内置外部公共 API 依赖；如需接入第三方 API，调用前必须先探活校验可用性，避免假设其永远可用（此前 public-apis 实测多接口已失效/需密钥，故移除该板块）。
- ❌ "未装专用技能就重写它" → ✅ 可选调用已安装专用技能（meeting-recap-tmeet/pptx/docx/xlsx/pdf 等内置类；若用户已装 OCR/合同审查类如 pdf-image-text-extractor/audit-new 也可选调用）；未装则走内置工作流或提示安装，而非重造。

**日程与精力管理（吸收自日程助手）**
- ❌ "今天会议排满就是高效" → ✅ 主动识别高密度会议时段，建议留出 ≥90 分钟专注块；连续工作超 4 小时提醒休息。
- ❌ "多个任务同天截止，硬扛" → ✅ 行动项追踪中检测截止日期堆叠与日程冲突，建议委托/推迟/拆分。
- ❌ "每天从零写日报/周报" → ✅ 晨间简报/周期报告自动从会议纪要、行动项、客户跟进中收集进度，用户只需补充和确认。

**异常处理与 FAQ 入口**
- ❌ "输出不满意让用户自己摸索" → ✅ 主动给出 3 个调整方向（更详细/更简短/换视角），并问"具体哪部分不满意"。
- ❌ "遇到异常回一句'可能吧'或'试试看'" → ✅ 统一话术：明确问题 → 给内置降级方案 → 给可选增强路径。详见 `references/faq.md`。
- ❌ "FAQ 散落在各处找不到" → ✅ 所有高频问题集中在 `references/faq.md`（20 个常见问题，每个都给可执行下一步），SKILL.md 首页也放了快速入口。
