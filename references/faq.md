# 常见问题速查（FAQ）

本文件回答使用「办公效率枢纽」时最容易卡住的 20 个问题。每个问题都给出**可直接执行的下一步**，不让你自己摸索。

---

## 01. 我没装任何专用 skill/连接器，这个 skill 还能用吗？

**能用。** 本 skill 的核心设计是"内置工作流保底，外部依赖只是可选增强"。

零依赖即可独立完成的场景（14 类）：会议纪要/待办提取、周报/日报、邮件草拟、文档总结、知识库沉淀、文档/纪要转 PPT（大纲）、客户/项目跟进、培训笔记、行动项追踪、财务/ERP 清洗对账、数据分析/透视、表格处理、合同内置审查、发票内置 OCR、PDF/图片文字提取（内置 OCR）、晨间简报。

只有两类场景需要额外条件：
- **可接入办公软件**（飞书/钉钉/企微/腾讯文档等）：需要连接器授权；未授权时可用"导出文件处理"兜底。
- **会议录音转文字**：需要转写能力（tmeet/语音转文字 skill/本机 ASR）；无依赖时按 `assets/audio-asr-guide.md` 给 4 档降级引导。

---

## 02. 连接器未授权怎么办？

**标准处理三步**：
1. 明确告知："当前未授权 XX 连接器（如 feishu/dingtalk/wecom/tencent-docs/kdocs/tmeet）。"
2. 给出启用路径："去 WorkBuddy 连接器管理页找到对应连接器，点'信任'启用。"
3. 立即降级："或先把协作平台的文档/表格导出发我，我按内置工作流处理。"

本 skill **不会伪造接口**或假装已接入。

---

## 03. 输出结果不满意，怎么让它改？

**直接说具体哪里不满意**，越具体改得越准。常用三选一：
- **更详细**："把行动项再细化，给出验收标准" / "把风险点逐条展开"。
- **更简短**："浓缩到一页纸" / "只保留决策点和行动项"。
- **换视角**："从财务视角再审查一遍" / "换成给老板看的汇报口吻"。

如果你不确定哪里不满意，本 skill 会主动给出 3 个调整方向让你选。

---

## 04. 上传的文件格式不支持怎么办？

**先转换格式，再上传**：
- 图片/扫描 PDF 需要 OCR → 直接上传，走 §13 预处理；如识别率低，会提示你粘贴文字。
- 音频文件 → 走 §16 会议录音转文字；无转写依赖时给 4 档引导。
- 压缩包/特殊格式（如 OFD、WPS 旧格式 .et/.dps）→ 先在本地另存为 .xlsx/.docx/.pdf 再上传。
- 文件损坏/无法解析 → 会明确告知问题，并让你换格式或粘贴文字内容。

---

## 05. 它和单一办公 skill（xlsx/pptx/docx/pdf）有什么区别？

| | 办公效率枢纽 | 单一专用 skill |
|---|---|---|
| **定位** | 统一入口 + 调度中枢 | 专精一个领域 |
| **怎么用** | 说需求，它识别并分流 | 直接指定格式操作 |
| **优势** | 一次串多个场景（纪要→邮件→周报） | 在单个领域更深度 |
| **关系** | 优先复用它们；没有时内置工作流兜底 | 被调用的增强组件 |

简单说：**办公效率枢纽是"指挥官"，xlsx/pptx/docx/pdf 是"特种兵"**。不会取代它们，而是让你一句话串起来用。

---

## 06. 会议录音转文字需要装什么？

按省事程度排序：
1. **最省事**：开会用腾讯会议 → 启用 `tmeet` 连接器，云录制自动转写。
2. **长期自动化**：在 SkillHub 装一个语音转文字 skill，本 skill 自动分流调用。
3. **数据留本地**：本机部署 `faster-whisper`，转写后把文字稿发给我。
4. **零门槛**：用任意现成工具（微信语音转文字、飞书妙记、讯飞听见等）转成文字稿，粘贴给我，我直接整理纪要。

**本 skill 不自带 ASR，也不会伪造转写。** 详见 `assets/audio-asr-guide.md`。

---

## 07. OCR 识别率低/乱码怎么办？

**处理顺序**：
1. 对不确定字词加 `[?]` 标记，不擅自猜测。
2. 复杂表格错位时，输出 Markdown 表格并说明"复杂表格建议人工核对"。
3. 手写体/潦草/低分辨率 → 明确说明"手写识别率低"，建议重新扫描或人工录入关键字段。
4. 如需更高识别率，可选装 `pdf-image-text-extractor` 类 OCR 专用 skill。

---

## 08. 合同审查能替代律师吗？

**不能。** 本 skill 只提供 AI 辅助参考，输出带免责声明。重大合同或复杂法律关系必须交执业律师/企业法务最终复核。

如果你已有 `audit-new` 类专业合同审查 skill，本 skill 会可选调用它增强审查深度；没有时走内置三视角（通用结构 + 采购 + 法务 + 财务）审查。

---

## 09. 发票识别后能直接入账吗？

**不能自动入账。** 本 skill 只产出：
- 结构化发票明细
- 凭证草稿（借/贷科目、金额、摘要）
- 异常清单（重号、税号、税率等风险）

最终入账需你在金蝶/用友等 ERP 中人工执行或复核。本 skill **不直连 ERP 数据库**。

---

## 10. 金蝶/用友能直连吗？

**不能直连。** 金蝶/用友是商业闭源 ERP，无公开 API。本 skill 只处理 ERP **导出的 Excel/CSV**：清洗、对账、生成凭证草稿、报表。导出文件处理方式零依赖可用。

如果你要求"直接登录 ERP 录凭证"，本 skill 会明确拒绝并给出凭证草稿方案。

---

## 11. 数据分析不会写公式怎么办？

**不用写公式。** 直接用自然语言说需求，例如：
- "帮我分析下这季度销售"
- "哪些子品类最赚钱"
- "按地区看月度趋势"
- "算一下同比环比"

本 skill 会自动识别意图 → 做数据体检 → 透视聚合 → 出图表 → 给结论。如果需求模糊，会先给出 5 类分析选项让你选，或默认做描述性统计 + Top N。

---

## 12. 生成的 PPT 没有图片/动画怎么办？

**正常。** 本 skill 生成的是内容大纲 + 可编辑 .pptx（若装了 `pptx` skill）或 Markdown/PDF 大纲（无依赖时）。不自动配图，复杂动画需人工调整，这是刻意边界——避免 AI 乱配图反而降低专业性。

建议流程：先生成内容大纲 → 确认内容 → 再人工配图/调动画。

---

## 13. 晨间简报怎么知道我今天日程？

**三种来源，按顺序**：
1. 你已授权的日历/待办连接器（如飞书/钉钉/企微日历）。
2. 当前会话里已经生成的会议纪要、行动项、客户跟进、周报等产物。
3. 你直接粘贴的文字待办/会议列表。

**不会偷偷读取你的日历隐私**。无授权时基于你主动提供的内容生成，并明确说明数据来源。

---

## 14. 敏感数据会被脱敏/外发吗？

**不会默认脱敏，也不会擅自外发。**

- 检测到身份证号/税号/银行账号等敏感字段时，**先问你要不要脱敏**，绝不默认遮盖。
- 对内场景（入账/报销/对账）默认保留原文，否则数字对不上。
- 对外分享/共享知识库时，才建议脱敏。
- 所有数据只在你的本地环境或已授权的连接器内处理，不会偷偷上传到第三方。

详见 `references/privacy_desensitization.md`。

---

## 15. 输出内容里有错误怎么办？

**直接指出错误，本 skill 立即修正。** 事实纪律是最高优先级约束：
- 所有产出标注置信度与来源，不确定标 `[待确认]`。
- 被纠正后立即改，并公开标注【已修正】，不嘴硬、不掩盖。
- 如果你发现数字/人名/条款有误，回复"第 X 点不对，应为 Y"即可。

---

## 16. 可以同时处理多个任务吗？

**可以。** 本 skill 鼓励"一句话串多场景"，例如：
- "整理会议纪要 + 提取行动项 + 草拟跟进邮件 + 进周报"
- "分析销售数据 + 生成 PPT 大纲 + 写一份汇报邮件"

数据链复用，避免重复输入。

---

## 17. 这个 skill 会帮我发邮件/发消息吗？

**不会。** 本 skill 只生成邮件/消息草稿，不替你发送、不登录你的邮件客户端、不调用你的企业微信/钉钉账号发消息。发送前必须人工复核并手动发送。

---

## 18. 文件太大上传失败怎么办？

**处理规则**：
- 单文件 < 50MB：直接处理。
- ≥ 50MB：建议分片/精简，或说明"将分块读取、分批产出"。
- 超大表格：可以先抽样上传前 1000 行做分析模板，确认后再全量跑。

本 skill 不会一次性载入大文件导致 OOM。

---

## 19. 为什么有时要我"具体说哪部分不满意"？

**为了给你可执行的下一步，而不是反复猜。** "不满意"太模糊时，本 skill 会主动给出 3 个常见调整方向（更详细/更简短/换视角），你选其一即可。这避免了 AI 盲目重写、你越改越烦。

---

## 20. 和"办公效率中枢"等竞品有什么区别？

本 skill 的核心差异：
1. **调度中枢**：先识别任务类型，再分流到最合适的内置工作流或可选增强能力，不是工具箱式堆砌。
2. **零依赖可用**：14 类核心场景无需任何外部 skill/连接器即可独立完成。
3. **事实纪律**：所有产出标注置信度、来源、不确定明说、被纠正立即改。
4. **完整闭环**：支持"输入 → 产出 → 沉淀知识库"，不是一次性的。
5. **国内办公合规**：金蝶/用友/发票/红涨绿跌/询问式脱敏等针对国内办公场景设计。

---

##  still 卡住？

按这个顺序排查：
1. 查 `references/capability-list.md` 看需求是否在 18 场景内。
2. 查 `references/examples.md` 照抄示例话术。
3. 回来看本 FAQ 对应条目。
4. 仍无法解决 → 直接说"我卡住了"，本 skill 会给出 3 个可执行的下一步。
