> **模板语言 / Template language**：本模板默认中文。当用户以英文（或其他语言）输入时，agent 应**把模板内容本地化为该语言**再输出，不强行用中文。
> This template is Chinese by default. When the user writes in English (or another language), the agent should **localize the content to that language** before output — never force Chinese.
> 占位符如 `[收件人]` `[事由]` 为通用结构，跨语言保持一致。/ Placeholders like `[收件人]` are structural and stay consistent across languages.

# 数据分析与透视模板 (Data Analysis & Pivot)

对应 WPS 表格的"数据透视 + 统计分析 + 可视化"能力。本文档定义输入约定、分析维度与输出结构，供调度中心在 §12 场景调用，实际计算与图表由 `xlsx` 技能（pandas/openpyxl）完成。

## 一、分析目标分类（先定方向，再动手）
- **描述性统计**：总量、均值、中位数、标准差、极值、Top N。
- **分组对比**：按维度（地区/品类/渠道/人员）分组聚合求和/计数/均值。
- **趋势**：按日/周/月滚动，同比（YoY）、环比（MoM）。
- **结构/占比**：构成比、帕累托（二八定律）、份额。
- **相关性/异常**：变量间关系、离群值、突然波动。

## 二、数据体检清单（必做，先于一切计算）
- 缺失值：标出空单元格及占比，决定填补/剔除。
- 重复行：识别并去重（保留业务主键唯一）。
- 类型校验：金额文本 → 数值；日期 → YYYY-MM-DD；百分比 → 小数。
- 单位统一：万元/元、件/箱，统一口径并注明。
- 口径说明：数据来源、期间、是否含退单/冲销。

## 三、透视与聚合结构
- **透视表四要素**：行字段 / 列字段 / 值字段（汇总方式：求和·均值·计数·最值）/ 筛选字段。
  - 示例：「行=地区，列=品类，值=销售额(求和)，筛选=2026Q2」。
- **分组聚合**：`GROUP BY 维度` + 聚合函数，输出分组明细 + 小计。
- **时间维度**：支持按日/周/月/季滚动；同比环比公式注明。

## 四、统计指标字段
| 指标 | 含义 | 用途 |
|------|------|------|
| 总量 / 合计 | SUM | 规模 |
| 均值 / 中位数 | AVG / MEDIAN | 典型水平（中位数抗极值） |
| 标准差 | STDEV | 波动/稳定性 |
| Top N / 末 N | 排序截取 | 重点/问题对象 |
| 占比 / 份额 | value / total | 结构 |
| 帕累托累计% | 累计求和 / 总和 | 二八定位 |

> 财务/股市相关图表遵循**红涨绿跌**；其他场景默认中性配色。

## 五、可视化输出
- 图表类型：柱状（对比）/ 折线（趋势）/ 饼环（占比）/ 热力（交叉强度）/ 散点（相关性）。
- 交付：建议 `xlsx` 输出「原始表 + 透视表 + 图表」三 sheet 可编辑文件；汇报场景转 `pptx` 单页图表。
- 每张图必须含：标题、坐标轴单位、数据期间、口径脚注。

## 五-B、交互式看板（可选增强，差异化记忆点）

当产出需要"一屏看全、可交互钻取"或用于汇报投屏时，在静态图表之外**可选**生成自包含看板，形成「xlsx 静态表 + 交互看板」双交付。这是获奖类办公 skill 的差异化点（对应 WPS「数据看板」用户期待），但不强制——无依赖场景下仍以 xlsx 静态图兜底。

- **自包含 HTML 看板（首选）**：用 ECharts（CDN 引入）生成单文件 `.html`，内嵌数据，双击即看。含：核心 KPI 卡 + 2~3 张可切换图表（柱/折/饼）+ 口径脚注。优势：无需联网/无需装库、可投屏、可交互。
- **SVG 信息图（轻量替代）**：对单一结论（如"9/17 子品类贡献 80% 利润"）用 SVG 画一页信息图，适合插入周报/邮件/汇报 PPT。优势：体积小、可嵌入文档。
- **触发条件（满足任一即建议，仍由用户确认）**：
  1. 用户说"看板 / dashboard / 一屏 / 投屏 / 汇报用"；
  2. 指标 ≥ 3 个且需横向对比；
  3. 产出要进周报/晨报/PPT 且含多图。
- **兜底纪律**：用户环境无浏览器/拒绝 HTML 时，回退纯 xlsx 图表 + Markdown 表，不卡死；CDN 不可用时把 ECharts 库内联或用占位提示。
- **事实纪律**：看板数字须与 xlsx 透视表一致，禁止为美观编造数据点。

## 六、结论与交付检查清单（强制）
- [ ] 分析目标已在开头声明
- [ ] 数据体检完成，缺失/异常已说明
- [ ] 透视/聚合口径与用户确认一致
- [ ] 图表单位、期间、口径标注完整
- [ ] 结论由数据支撑，未臆造数字
- [ ] 已注明数据局限（样本量、来源、未含项）
